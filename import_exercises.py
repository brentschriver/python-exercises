from function_exercises import calculate_tip
from itertools import combinations as combo, permutations as perm, product 


print(list(combo('ABC123',2)))
print(len(list(combo('abcd',2))))
print(len(list(perm('abcd',2))))

